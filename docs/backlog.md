# Backlog

- tight coupling between storage layer and things that need to use it, need an intermediate "store" struct, should not interface with db so directly
- styling
- code review as of Apr 05, need to go over again when not tired
- some sort of metadata standardization, normalization, parsing from epubs...etc.
- ci ... build? release? etc.
